Leetcode 345
1. 把string格式的字符串中的元音字母换位,其他字母不变
2. 首先这是string格式,我们必须先把其转为list才能正常换位,
   之后建立包含大小字母的元音map(‘aeiouAEIOU’), 如果字符在map中,由于并非所有字母参与换位, 所以要利用start和end一组变量来换位
3. 当start < end时, 如果s[start], s[end]都在map中,那么互换
   如果只有s[start]在map中, 那么end - 1向前一项移动以进行检查
   如果只有s[end]在map中, 那么start + 1向后一项移动以进行检查
4. 返回拼合后的结果 ''.join(s)， 其中''代表着连接处需要用到的符号, 默认是空, 直接拼合
   如果是'->'则是右箭头
