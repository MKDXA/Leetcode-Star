Leetcode 476
1. 将数字变为二进制,返回该二进制的补数
2. bin()在Python3中会自动在开头加上0b, 所以要舍弃前两位, bin(num)[2:]
3. 取得长度len(bin(num)[2:]), 即为2的次方数, 由于题目中要求首位不能是0,所以是(2 ** (bin(num)[2:])) - 1
4. 最后就是求补位, 按数学公式,将2的次方作为num的异位运算符即可
   return num ^ (2 ** len(bin(num)[2:])) - 1)
