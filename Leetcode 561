Leetcode 561
1. 将无序数组中的数字两两分组,取每组的最小值相加为和,求结果的最大值
2. 把数组变成有序,那么最小值相加就等于把每个小组的最左边的值相加,也就是最大值
3. 有序化数组: num.sort()
4. 由于是一组两个数,所以遍历是需要设定step为2: for i in range(0, len(nums), 2):
5. nums[i]就是每组的最小值,将每一个最小值相加  result += nums[i]
6. 返回result
